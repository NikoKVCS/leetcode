package main

import (
	"fmt"
	"testing"
)

func convert(s string, numRows int) string {

	list := make([][]rune, 0)
	for {

	}
	col := make([]rune, numRows)
	i := 0
	for id, c := range s {
		if i == numRows || id == len(s) {

		}
	}
	return ""
}

func TestLeetcode6(t *testing.T) {
	fmt.Println(convert(" ", 3))
}
